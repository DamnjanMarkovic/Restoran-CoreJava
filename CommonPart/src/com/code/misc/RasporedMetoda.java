package com.code.misc;

public class RasporedMetoda {


    /*

    OffersForm:

    listAllIngredient = returnListIngredientsBasdOnUser(1);
        listOffers = returnListRestaurantMenues();

        AVAILBLEMENU FORM
        public static List<Offer> returnListAvailableMenues(int idRestaurant, List<Offer> listAllMenues) {

         public static List<ImageRestaurant> returnImages() {


    ORDERFORM:

        listAvailableMenues = returnListAvailableMenues(restaurant.getId_restaurant(), listAllMenues);
        cbFood.setRenderer(new ImagesTextRenderer());
        listIngredient = IngredientForm.returnListIngredientBasedOnRestaurant(restaurant.getId_restaurant());
    private boolean checkifAvailable(Restaurant restaurant, List<Order> listOrder) {
    private List<Offer> returnListAvailableMenues(int idRestaurant, List<Offer> listAllMenues) {

    if (rstm.getRestaurant_menu_type().equals("ALCOHOLIC_DRINK")) {
        for (ImageRestaurant image1: images) {
            if (rstm.getId_restaurant_menu()==image1.getId_offer()){
                ImageIcon imageIcon = new ImageIcon(new ImageIcon(image1.getImageLocation()).getImage().
                        getScaledInstance(100, 80, java.awt.ImageRestaurant.SCALE_SMOOTH));
                cbAlcoholicDrink.addItem(new ImagesNText(new ImageIcon(imageIcon.getImage()), rstm.getRestaurant_menu_name()));
            }
        }

    }

    String offerName = cbFood.getModel().getSelectedItem().toString();

INGREDIENT FORM

public static List<Ingredient> returnListIngredientBasedOnRestaurant









    WAITER ENTRY FORM:
        List<Restaurant>restaurantsList = RestaurantForm.getRestaurantsList(user);
        listAllMenues = RestaurantForm.returnAllListRestaurantMenues();
        restaurant = restaurantsList.stream().findFirst().get();
    public static DinningTable returnDinningTable(int table_number, int id_restaurant) {}






     */
}
